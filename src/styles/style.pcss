:root {
    font-family: Inter, system-ui, Avenir, Helvetica, Arial, sans-serif;
    font-weight: 400;
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    --padding: 20px;
    --color: #676767;
    --highlight-color: #242424;
    --transition-time: 0.5s;
}

a {
    font-weight: 500;
    color: #646cff;
    text-decoration: inherit;
}

a:hover {
    color: #535bf2;
}

body {
    margin: 0;
    display: flex;
    place-items: center;
    min-width: 320px;
    min-height: 100vh;
    overflow: hidden;
}

.city-heading {
    text-wrap: nowrap;
}

ul, li {
    list-style: none;
    margin: 0;
    padding: 0;
}

nav {
    width: 90svw;
    margin: 0 auto;
    border-bottom: solid 1px var(--color);


}

.wrap {
    width: 100%;
    margin: 0 auto;
}

#cities {
    display: flex;
    align-items: center;
    gap: 40px;
}

.city {
    flex-grow: 1;
    text-align: center;
    user-select: none;

    box-sizing: border-box;

    h3 {
        /* disabling pointer events to simplify things a bit since we'd still have to counter the width of the heading regardless.*/
        pointer-events: none;
        font-size: 1vw;
        color:var(--color);
        transition:color var(--transition-time) ease;
        font-weight:400
    }

    &:hover {
        cursor: pointer;
    }

    &.clicked {
        h3 {
            color:var(--highlight-color);
            transition:color var(--transition-time) ease;
        }
    }
}

/*=== Highlights what's currently selected === */
#selector {
    height: 2px;
    width: 20px;
    background: red;
    transition: transform var(--transition-time) ease, width var(--transition-time) ease, opacity var(--transition-time) ease;
}